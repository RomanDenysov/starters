import styled from "styled-components"

const HeaderContainer = styled.div`
    
`

const Header = () => {
  return (
    <HeaderContainer>
        Hello World!
    </HeaderContainer>
  )
}

export default Header